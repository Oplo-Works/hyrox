# v8.1.1 Workflow Adoption Report

- Task ID: workflow-adoption-v8.1.1
- Target workflow: v8.1.1-solo
- Adoption branch: `chore/adopt-workflow-v8.1.1`
- Adoption base HEAD: `2663a83e3096e4a67c798793858216780b0097ed`
- Date (UTC): 2026-07-11

## Executive Summary

The repository was migrated from a **LEGACY_V7** ("Claude × Codex × GLM") workflow to
**v8.1.1-solo**. The 25-file bundle was verified (integrity, no path traversal/symlink/executable,
ZIP↔master consistency). 19 workflow-owned files were installed byte-for-byte; the 6 project-owned
files (CLAUDE.md, AGENTS.md, PROJECT_SCOPE, MODEL_RUNTIME_PIN, HANDOFF, DEV_LOG) were reconciled
into the v8.1.1 schema while preserving every verified project fact. The active v7 manual was
removed and, together with the other transformed files, archived byte-for-byte. **No application
source, test, dependency, schema, infrastructure, or deployment file was changed.** Baseline and
post-adoption `npm run build` both PASS.

## Detected Source Workflow

- Classification: **LEGACY_V7** — confidence: high.
- Evidence: `docs/AGENT_WORKFLOW.md` was a single monolithic source-of-truth pointing to
  `docs/AI_Coding_Agent_Workflow_v7.md` (title "Claude × Codex × z.ai/GLM Edition"); v7 HANDOFF
  used `Last Commit` fields and per-session "commit + push" rules; bootstrap files (CLAUDE.md,
  AGENTS.md) used v7 three-model language ("GLM builds, Codex makes it run, Claude judges").
- The project was, one session earlier, itself migrated v6→v7; the v6 manual was already deleted
  and only survives in Git history. Current source state is unambiguously v7.

## Pre-Adoption Repository State

- Branch: `main` (protected) @ `2663a83`; upstream `origin/main`; worktree **REPO_CLEAN** (no
  dirty/untracked/staged user changes). Adoption branch `chore/adopt-workflow-v8.1.1` created from `2663a83`.
- Remote: `https://github.com/Oplo-Works/hyrox.git`.
- Push side effects: pushing `main` triggers a Netlify production deploy; the adoption task branch does not.
- Baseline `npm run build`: **PASS** (recorded pre-adoption; not a repair).
- Pre-adoption SHA256 of transformed files recorded in the archive MANIFEST.

## Package Verification

- ZIP SHA256: `6867f85f1d32fc9af9cf7659fa16d7395b48ae78c1b60af673bb26b3bd6045d6`
- Master SHA256: `6ffa19ad8d267db4c5112298bc986c045ed05666eb90b21e85e3c0cefce58e77`
- Extracted to a temp dir outside the repo. **25 files** confirmed under a single top-level dir.
  No path traversal, absolute paths, symlinks, or executable content.
- ZIP↔master consistency: master contains the distinctive markers of the bundle (v8.1.1-solo,
  WF:SPEC_PLAN stage map, PIN runtime IDs, GIT_SAFETY worktree states, REVIEW ARTIFACT_READ_ONLY).
- Installed workflow-owned files verified byte-identical to the bundle (per-file SHA256 match).

## Archived Legacy Files

- Archive: `docs/archive/workflow/pre-v8.1.1-20260711T135301Z/` + `MANIFEST.md`.
- Byte-for-byte snapshots (verified SHA256 match) of: CLAUDE.md, AGENTS.md, docs/AGENT_WORKFLOW.md,
  docs/AI_Coding_Agent_Workflow_v7.md, docs/PROJECT_SCOPE.md, docs/HANDOFF.md, docs/DEV_LOG.md, README.md.
- Git history remains primary history; the archive is an adoption-time snapshot.

## Installed Workflow-Owned Files (19, verbatim)

WORKFLOW_INSTALL.md; docs/AGENT_WORKFLOW_CORE.md; docs/MODEL_RUNTIME_SNAPSHOT.md;
docs/workflow/{BUILD,CLOSE,GIT_SAFETY,MICRO,PLAN,PRODUCT,REVIEW,SPEC,TEST}.md;
docs/{features,product,architecture,design,handoff_history}/README.md;
templates/{gitignore.workflow-snippet,env.example.workflow-template}. Active v7 manual removed
(archived). No deprecated semantics kept in active copies; no project model names hardcoded in CORE/playbooks.

## Project-Owned Merge Decisions

| File | Decision | Note |
|---|---|---|
| CLAUDE.md | PROJECT_OWNED_MERGE | v8.1.1 bootstrap block + clearly-marked Project-Specific Instructions (HYROX/bilingual/palette guardrails); old v7 bootstrap removed |
| AGENTS.md | PROJECT_OWNED_MERGE | same as CLAUDE.md |
| docs/AGENT_WORKFLOW.md | SUPERSEDED pointer | facts migrated to PROJECT_SCOPE; content archived |
| docs/PROJECT_SCOPE.md | PROJECT_OWNED_MERGE | v8.1.1 schema; all v7 scope facts preserved; HUMAN-OWNED policy filled from adoption-prompt authority |
| docs/MODEL_RUNTIME_PIN.md | NEW (from template) + honest observations | all rows CANDIDATE; only session-observed model IDs recorded |
| docs/HANDOFF.md | rebuilt | v8.1.1 schema from actual repo state |
| docs/DEV_LOG.md | preserved + boundary note (+ CLOSE event at close) | history byte-for-byte; not rewritten |
| .gitignore | additive merge | added `.env.*`, `!.env.example`, `*.key`, `secrets/`, `.local/`; existing rules untouched |
| .env.example | created (placeholder-only) | was missing |
| README.md | workflow-link repair only | stale v6 file-tree ref + AGENT_WORKFLOW link → v8.1.1; project content unchanged |

## Project Scope Reconciliation

All v7 scope facts preserved (MVP scope, out-of-scope, users, workflows, data/security rules,
role visibility, HYROX usage rules). Added: validation commands table (build required; lint/secret-scan),
environment/deployment facts (Netlify), and the HUMAN-OWNED policy filled with the values this
adoption prompt authorizes. Status = **READY_FOR_APPROVAL** because runtime approvals and paid-use
caps are user decisions (see Remaining Human Inputs).

## Runtime Pin Reconciliation

Created from the v8.1.1 template. **All rows CANDIDATE.** Only session-observed facts recorded:
Claude Code active model `claude-opus-4-8` (this adoption) and `claude-fable-5` switchable in-session
— model-id level only. Codex and z.ai/GLM runners were **not observed** this session. The legacy v7
model-pin claims (Opus 4.8 Pro $20, GPT-5.5, GLM 5.2) are recorded as unverified claims, not evidence
(note the GPT-5.5 vs snapshot GPT-5.6 discrepancy). No approved-runtime list populated (none APPROVED yet).

## Completed Work Reconciliation

`docs/migration/LEGACY_WORK_INDEX.md` groups 8 meaningful milestones (MVP build, Netlify config,
Kakao link, header typography, Codex review, scroll energy, Nitro theme, v7 governance) with honest
classes (VERIFIED_BY_EXISTING_EVIDENCE / VERIFIED_DURING_ADOPTION / COMPLETED_BUT_UNVERIFIED /
SUPERSEDED). No retroactive SPEC/PLAN/approval/evidence invented.

## Active Work Reconciliation

No unfinished in-progress application task. The most recent feature (Nitro theme, `fc05aeb..2663a83`)
is complete and already on `origin/main`. Remaining items (OG image, Next.js security patch, sticky-CTA
focus, 320px polish) are **backlog**, not active work; each future change starts a new v8.1.1 task
(WF:SPEC_PLAN or WF:MICRO by risk). This adoption implemented no application feature.

## Future-Work Enforcement

Confirmed in ACTIVE files: v8.1.1 is the only active workflow authority; `docs/AGENT_WORKFLOW.md` is a
SUPERSEDED pointer only; no bootstrap points to a legacy manual; no active file requires reading a full
old manual each session; no literal `<CURRENT-STAGE>.md`; no `FIX_REQUESTED` review mode; reviewer
decisions are PASS/CHANGES_REQUESTED/NEEDS_HUMAN_APPROVAL only (APPROVED_WITH_RISK is Human-only);
High PASS routes to Human, not directly to CLOSE; model routing lives only in MODEL_RUNTIME_PIN; HANDOFF
grants no authority. Regression greps: all PASS.

## Policy Applied

- Local Commit: `AUTO_LOCAL_AFTER_TEST`, task-owned paths only, explicit-path staging (no `git add .`).
- Push: `AUTO_AT_CLOSE` to `origin/chore/adopt-workflow-v8.1.1` (non-protected) only. `main` is protected
  and deploy-triggering → not pushed. No PR created. No deploy/release/notification/paid side effect.
- Git: branch isolation used; no merge/rebase/force-push/reset/stash/restore/clean.
- User changes: none existed (REPO_CLEAN); none altered.

## Validation

| Check | Result |
|---|---|
| ZIP integrity / 25 files / no traversal/symlink/exe | PASS |
| Installed files == bundle (SHA256) | PASS |
| Master↔ZIP consistency | PASS |
| Markdown fences balanced (active + migration) | PASS |
| Stage map → all 9 playbooks resolve | PASS |
| Bootstrap read order present (CLAUDE.md/AGENTS.md) | PASS |
| Single active workflow authority (no active full manual) | PASS |
| Legacy regression greps (`<CURRENT-STAGE>`, FIX_REQUESTED, v5/v7 authority, hardcoded models, push-every-session, reviewer APPROVED_WITH_RISK) | PASS |
| Git ownership: task-owned paths only, no app/test/dep/deploy file changed | PASS |
| Secret scan (staged-diff review) | PASS — no secrets/tokens/PII introduced (.env.example placeholders only) |
| `npm run build` (post-adoption) | PASS |
| `npm run lint` | PASS (0 warnings/errors) |
| Application source unchanged | PASS |

## Independent Review

- Mode: ARTIFACT_READ_ONLY. Reviewer: independent subagent (separate context; did not perform the adoption).
- Decision: **PASS**. Findings: P0=0, P1=0, P2=0, P3=1. Full artifact: `docs/migration/V8.1.1_ADOPTION_REVIEW.md`.
- P3 (non-blocking, addressed): archive MANIFEST SHA256 values are working-tree (CRLF) hashes (repo `core.autocrlf=true`), not reproducible via `git show|sha256sum`; a clarifying note was added to MANIFEST.md and archive integrity is proven at the Git blob level (v7 manual moved as R100 rename).
- Reviewer independently re-verified: 19 workflow-owned files byte-identical to bundle; zero application/build/deploy files in range; single active authority; no legacy regressions; PIN all-CANDIDATE; SCOPE READY_FOR_APPROVAL with protected-branch=NEVER; DEV_LOG preserved; no secrets.

## Git and Publish

- Adoption artifact commit: `95e9253` — `chore(workflow): adopt v8.1.1 solo workflow` (explicit-path staged).
- Close metadata commit: `docs(handoff): close v8.1.1 workflow adoption` (REVIEW + MANIFEST P3 note + final HANDOFF + DEV_LOG event + report finalize).
- Range: `2663a83..<close head>`.
- Push: one push to `origin/chore/adopt-workflow-v8.1.1` (non-protected task branch). NOT pushed to `main` (protected + deploy-triggering). No PR created.

## Remaining Human Inputs

1. Approve `docs/MODEL_RUNTIME_PIN.md` runtime rows (subscriptions, observed models, permissions) → flip used rows to APPROVED and list them in PROJECT_SCOPE §5.
2. Set paid-use monthly caps (Fable usage credits; OpenAI extra credits) or confirm ASK_EACH_TIME.
3. Confirm repository classification = Public.
4. (Optional) Decide whether to merge `chore/adopt-workflow-v8.1.1` into `main` (a deploy-triggering action → ASK_ALWAYS).

## Final Outcome

**DONE** — v8.1.1-solo is the only active workflow authority; independent review PASS; adoption
branch committed and pushed. The adoption *task* is complete. Before the first real feature task,
the Human must resolve the Remaining Human Inputs (approve MODEL_RUNTIME_PIN rows, set paid-use caps,
confirm repo classification), and separately decide whether to merge `chore/adopt-workflow-v8.1.1`
into `main` (a deploy-triggering action requiring explicit approval).
