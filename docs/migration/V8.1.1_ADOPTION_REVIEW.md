# Review: v8.1.1 Workflow Adoption

- Mode: ARTIFACT_READ_ONLY
- Reviewer: independent subagent (general-purpose; did not perform the adoption)
- Review Range: 2663a83..95e9253 (full head `95e92538c180ca67cfd936824943ae44de7b7201`)
- SPEC/PLAN/EVIDENCE: docs/migration/V8.1.1_ADOPTION_REPORT.md
- Risk: Standard
- Decision: PASS

## Findings

| Sev | Location | Impact | Evidence | Recommended fix | Status |
|---|---|---|---|---|---|
| P3 | docs/archive/workflow/pre-v8.1.1-20260711T135301Z/MANIFEST.md (SHA256 column) | Cosmetic reproducibility gap: the "SHA256 (pre-adoption)" values are the Windows CRLF working-tree hashes (e.g. CLAUDE.md `5189453f…`), so `git show 2663a83:CLAUDE.md \| sha256sum` on a normalized checkout returns a different value (`ca8e4abb…`). Byte-for-byte archive integrity is NOT affected — proven identical at the git-blob level. | `git rev-parse 2663a83:CLAUDE.md` == `git rev-parse 95e9253:.../CLAUDE.md` == `1e4de9f95fea…` (identical); same identity holds for AGENTS.md, AGENT_WORKFLOW.md, PROJECT_SCOPE.md, HANDOFF.md, DEV_LOG.md, and the v7 manual. `core.autocrlf=true`, no `.gitattributes`. | Optional: note in MANIFEST that listed SHA256s are CRLF working-tree hashes, or regenerate them from `git show <blob>` for cross-platform reproducibility. Non-blocking. | OPEN |

## Checks run directly

- `git diff 2663a83..95e9253 --name-only` / `--name-status` → 40 paths; only docs/**, governance/root bootstrap (CLAUDE.md, AGENTS.md, README.md, WORKFLOW_INSTALL.md), templates/**, .env.example (A), .gitignore (M). No app/build/deploy path present.
- Defensive grep of range name-list for `^(app/|components/|data/|package(-lock)?\.json|next\.config|netlify\.toml|tailwind\.config|tsconfig|postcss\.config|\.eslintrc)` → NONE. Confirms NO APPLICATION CHANGES.
- SHA256 of all 24 bundle files vs repo copies → 19 IDENTICAL (all workflow-owned: WORKFLOW_INSTALL.md, docs/AGENT_WORKFLOW_CORE.md, docs/MODEL_RUNTIME_SNAPSHOT.md, docs/workflow/{BUILD,CLOSE,GIT_SAFETY,MICRO,PLAN,PRODUCT,REVIEW,SPEC,TEST}.md, docs/{architecture,design,features,handoff_history,product}/README.md, templates/{env.example.workflow-template,gitignore.workflow-snippet}); the 6 DIFFERS files are exactly the project-adapted set (CLAUDE.md, AGENTS.md, docs/DEV_LOG.md, docs/HANDOFF.md, docs/MODEL_RUNTIME_PIN.md, docs/PROJECT_SCOPE.md). All check-1 named files (CORE, GIT_SAFETY, REVIEW, CLOSE, MODEL_RUNTIME_SNAPSHOT, templates/*) IDENTICAL.
- `find docs -name "AI_Coding_Agent_Workflow_v*.md"` + `git ls-files | grep AI_Coding_Agent_Workflow` → only the archived v7 manual exists; no active version-labeled manual.
- Read CLAUDE.md, AGENTS.md → both titled "…Bootstrap (v8.1.1-solo)" with read-order CORE → PIN → SCOPE → HANDOFF → stage playbook → GIT_SAFETY → SPEC/PLAN; no pointer to a full legacy manual; v6/v7 labeled archived history only.
- Read docs/AGENT_WORKFLOW.md → "Superseded Workflow Pointer", Status SUPERSEDED, points to CORE; not an active manual.
- Regression greps over CLAUDE.md/AGENTS.md/docs/AGENT_WORKFLOW.md/docs/AGENT_WORKFLOW_CORE.md/docs/workflow: `<CURRENT-STAGE>` NONE; `FIX_REQUESTED` NONE; push-after-every-session NONE; hardcoded model names (`claude-*`/`gpt-*`/`glm-*`/opus/sonnet/fable-N) in CORE + docs/workflow → NONE.
- Read docs/workflow/REVIEW.md + docs/AGENT_WORKFLOW_CORE.md §6 → Reviewer Decision enum = PASS | CHANGES_REQUESTED | NEEDS_HUMAN_APPROVAL only; Human Decision = APPROVED | APPROVED_WITH_RISK | REJECTED (APPROVED_WITH_RISK appears only as Human Decision); High PASS routes to NEEDS_APPROVAL → Human, not straight to CLOSE (REVIEW.md routing table + CORE §8).
- Archive integrity: `git diff 2663a83:CLAUDE.md 95e9253:<archive>/CLAUDE.md` empty; `git rev-parse` blob IDs identical for all 6 transformed docs + confirmed working-tree SHA match for the v7 manual against `git show 2663a83:…`. MANIFEST.md lists path/archive-path/SHA256/git-status/version/reason per file.
- Secrets: `git diff 2663a83..95e9253` added-line scan for secret/token/key/PEM/AKIA/ghp_/sk- patterns → only documentation prose and `.gitignore` `secrets/` entries; no secret values. `git show 95e9253:.env.example` → placeholder comments only.
- `git rev-parse chore/adopt-workflow-v8.1.1` = `95e9253…` (== range head); `git status --porcelain` clean before writing this file.

## Evidence read but not re-run

- docs/migration/V8.1.1_ADOPTION_REPORT.md — adoption evidence/claims; cross-checked against my direct results and found consistent (19 verbatim files, 6 project merges, no app changes, all-CANDIDATE PIN, READY_FOR_APPROVAL scope, regression greps PASS). Its `npm run build` / `npm run lint` PASS claims were not re-run (out of ARTIFACT_READ_ONLY review scope and no app code changed in range).
- docs/archive/workflow/pre-v8.1.1-20260711T135301Z/MANIFEST.md — archive record (paths, reasons, git status).
- docs/MODEL_RUNTIME_PIN.md, docs/PROJECT_SCOPE.md, docs/HANDOFF.md, docs/DEV_LOG.md — read in full for honesty verification (see below).

## Scope / Regression / Security / Permission

- Scope: Correct. The range is confined to workflow/governance/root-bootstrap/docs/templates/.env.example/.gitignore/README.md. Zero application source, test, dependency, build, or deploy files touched (independently confirmed). Branch head equals review range head; worktree clean.
- Regression: No legacy v7 artifacts leaked into active authority. Single active workflow (v8.1.1-solo) via CORE + docs/workflow; AGENT_WORKFLOW.md is a superseded pointer; bootstraps carry the correct v8.1.1 read-order and do not route to a full legacy manual. Reviewer/Human decision enums, High-PASS→Human routing, and no-hardcoded-model rules all hold.
- Security: No secrets/tokens/keys/PII introduced. .env.example is placeholder-only; .gitignore change is additive secret-hygiene hardening (`.env.*`, `!.env.example`, `*.key`, `secrets/`, `.local/`). PROJECT_SCOPE HUMAN-OWNED policy marks main/master/production/release as NEVER direct-push, deploy/paid actions as ASK, and "Approved Runtime IDs: none yet".
- Permission: Adoption stayed within artifact/governance scope; no protected-branch push, no deploy, no paid action, no app mutation. HANDOFF matches actual branch (chore/adopt-workflow-v8.1.1), base 2663a83, worktree TASK_DIRTY_ONLY, Status READY_FOR_REVIEW, and explicitly grants no new authority. MODEL_RUNTIME_PIN keeps all rows CANDIDATE and records only session-observed Claude Code models (claude-opus-4-8 / claude-fable-5) while marking Codex/GLM and all billing as NOT observed.

## Residual Risk and Transition

- Residual risk: Low. Only one cosmetic P3 (MANIFEST SHA256 values are CRLF working-tree hashes, not canonical git-blob hashes); archive integrity is proven at the blob level. Substantive human decisions remain deferred and are correctly flagged (PIN approval, paid-use caps, repo classification, optional main-merge/deploy) — these are expected post-adoption inputs, not defects.
- Next transition: CLOSE
- Required action: Proceed to WF:CLOSE — finalize HANDOFF/DEV_LOG and push once to `origin/chore/adopt-workflow-v8.1.1` only (never to protected `main`, which triggers a Netlify deploy). Optionally apply the P3 MANIFEST note as a non-blocking follow-up.
