# Placeholder names only. Never put real secrets in this file.
# This project (static Next.js landing page) currently needs no runtime secrets.
# Add placeholder keys here if/when an integration is introduced, e.g.:
# EXAMPLE_API_KEY=
# EXAMPLE_BASE_URL=
